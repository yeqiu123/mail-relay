services:
  mail-relay:
    build: .
    container_name: mail-relay
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/data
    ports:
      - "127.0.0.1:8765:8000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=5)"]
      interval: 30s
      timeout: 8s
      retries: 3
      start_period: 15s

  mail-relay-worker:
    build: .
    container_name: mail-relay-worker
    command: ["python", "-m", "app.worker"]
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/data
    healthcheck:
      test: ["CMD", "python", "-c", "from app.config import config; from app.security import Vault; from app.store import Store; raise SystemExit(0 if Store(config.database_path, Vault(config.encryption_key)).service_heartbeat_is_fresh('worker', 45) else 1)"]
      interval: 30s
      timeout: 8s
      retries: 3
      start_period: 30s
    depends_on:
      mail-relay:
        condition: service_healthy
